FROM golang:1.25.0-bookworm

ENV DEBIAN_FRONTEND=noninteractive

# Builder base: install toolchain deps once.
RUN apt-get update && \
    apt-get install -y --no-install-recommends ca-certificates gcc libc6-dev && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

